# ODCA — Swift/SwiftUI implementation

See the repository root `README.md` for what ODCA is and how to use it,
`../REQTS.md` for the specification, and `../REQ-swift.md` for
implementation notes. macOS 14+ with the Xcode toolchain.

All commands below run from this `swift/` directory. The shortcut is
`./run odca <file>` (or `run odca-select`, `run odca-evolve`, `run test`), which builds
release on first use and then runs the binary; it can be called from
anywhere, as `swift/run` from the repository root.

## Run

```sh
swift run -c release odca ../interesting.odca          # play a file of pairs (--fullscreen, --3 / --2 / --1 optional)
swift run -c release odca a.play b.play --shuffle      # play scripts and odca files, in a fresh order each pass
swift run -c release odca ../my-pairs.odca --longest   # play the seeds odca-evolve recorded, in a fixed-width window
swift run -c release odca-select ../my-pairs.odca      # compose pairs into a file (--3 / --2 / --1 optional)
swift run -c release odca-evolve ../my-pairs.odca --cells 600 --time 300   # find each rule's longest-lived seeds (no window)
```

Use the release build for viewing. A debug build (`swift run odca`) keeps
every bounds check and skips inlining in the per-frame pixel loop, and the
animation is visibly choppy even on a fast Mac; release is smooth. The
first release build takes about a minute, then it launches at once.

`--help` on any program prints its flags and keys. `odca-evolve` is
Swift-only for now; it uses every processor, so run it in release. The play script
parser is the `CShow` target: C generated by `../script/regen` from the
flex/bison grammar in `../script/`, built like any other target, so a
clone needs neither flex nor bison.

## Tests

```sh
swift test
```

This runs the property/session tests plus the conformance runner, which
checks the engine against the shared golden vectors in
`../conformance/vectors.json`.
